---
name: photo-doodle
description: 在照片上程序化手绘马克笔涂鸦（白色/彩色、水彩/蜡笔/荧光/闪光/毛绒多种笔刷），输出静态成品图 + "一笔一笔画出来"的逐笔绘制过程视频（MP4）。当用户想给照片/自拍/人像加手绘涂鸦、贴纸感装饰、猫耳猫须变小猫、毛绒尾巴、颜文字、像素贴纸、天使光环、爱心星星、手账标注、涂鸦短视频，或提到 photo doodle、照片涂鸦、给照片画点什么、涂鸦动画时使用。设计必须从照片具体内容出发（先列名词清单→定主导元素家族→自审），严禁默认撒花撒星配 golden hour 类套话——SKILL.md 第 1-4 步是防套路硬闸，务必逐步执行。三条硬闸：每个元素必须写出依据照片什么、一版最多一句文字（英文+手写体）、笔画粗细至少 3 档且要有 W_BOLD 级粗笔。标准流程：若有生图模型（Codex 等）先生成涂鸦设计稿作布局参考，再用引擎真矢量笔迹在原图上复刻；没有则直接自行设计。渲染纯 PIL+ffmpeg 本地完成，最终照片本身不被修改（涂鸦是叠加层）。
---

# photo-doodle：照片手绘涂鸦 + 过程视频

在照片上生成像真人用马克笔画的涂鸦：抖动笔迹、圆头笔画、笔锋收细、暗色底影、柔光。每个版本同时产出静态 JPG 和逐笔绘制过程 MP4（观众能看到每一笔被画出来，文字按书写方向擦入）。

**铁律：原照片一个像素不动，所有涂鸦必须是引擎的真矢量笔迹逐笔画出。** 禁止像素揭示/图像差分显现类方案。

## 标准流程（先判断走哪条路）

1. **有生图模型可用**（Codex CLI 等）→ 先让 AI 基于目标照片生成一张涂鸦设计稿（**仅作元素布局参考**），叠网格量出各元素归一化坐标，再用引擎矢量笔迹在原图上复刻。完整步骤（生成命令、量坐标方法、并排比对质检）见 `references/design-replica.md`——**走此路径前必读**。

   ⚠️ **人物特写照片：生图模型几乎必然返回撒花撒星套路**（实测两次：一次回了 6 朵花 + 14 颗星 + "Sunshine girl"，一次回了 14 颗星 + 3 朵花 + 笑脸云，落点还是内容无关的——花贴在衣服上、星压在头发上看不见）。这种情况下**只把它当留白地图用（哪里空、哪里可放东西），不要在它基础上做删减**——"花从 6 朵减到 3 朵、星从 14 减到 4"只会得到一个能通过所有数值检查的**稀释版套路**。设计仍要按下面第 1-4 步从零做。
   **复刻流程与防套路闸冲突时，以防套路闸为准**：design-replica.md 讲的是"如何精确复刻一张设计稿"，它假设那张稿子值得复刻；纪实/风景稿常常值得，人物特写稿基本不值得。
2. **没有生图模型** → 不用参考，直接按下面的工作流程自行构思设计并画。

两条路都要求：设计适配照片本身的风格与美感——色调、情绪、留白位置决定用色、元素密度与落点，不要把同一套元素硬套到所有照片上。

## 工作流程

### 1. 读照片，写"内容清单"（防套路第一道闸）

用 Read 亲眼看照片，然后把下面四项**写出来**（写进你的回复，不是心里过一遍）：

- **≥3 个具体名词**：照片里真实存在、能用手指着说出名字的东西（伞骨、石阶、墨镜、麦克风、马鬃、晒衣绳、笔记本电脑、白板……）。不许写"光线""氛围""青春感"这类抽象词。**逐个标注"这个我会不会真给它画元素"**——宁可 3 个真用得上，不要凑满 5 个然后一半闲置（真实教训：为凑数写了"领口抽绳结"，最终设计里根本没用到它）。
- **1 个动作或姿态**：她指着脸颊 / 他举着手机 / 她握着缰绳 / 他在讲话。
- **现场光与色**：暖金逆光 / 黄昏蓝粉渐变 / 正午高反差蓝天 / 室内白墙冷光。同时**定下配色**：取画面主色调，本版只用它的同温色系 + 白（暖调照片就 YELLOW/LEMON/PEACH/CORAL/WHITE，冷调就 SKY/BLUE/LAVENDER/MINT/WHITE）。冷暖混用会和现场光打架。
- **留白区 + 实测亮度**：哪几块是天空/墙面/地面，各自的归一化坐标范围，**以及每块的实测平均亮度**。别凭眼估——亮度值直接决定文字用什么颜色才看得清，这是白笔迹在亮背景上糊掉的唯一防线：

```python
from PIL import Image; import numpy as np
im = np.asarray(Image.open(SRC).convert('L'), dtype=float)
H, W = im.shape
for name, (x0, y0, x1, y1) in {'左下石阶': (0.05, 0.72, 0.35, 0.95), '右上墙': (0.75, 0.05, 0.98, 0.30)}.items():
    patch = im[int(y0*H):int(y1*H), int(x0*W):int(x1*W)]
    print(f'{name}: L={patch.mean():.0f}')   # L<110 白字安全；L>150 白字必糊，改用饱和色或深色
```

这份清单是后续设计的**唯一素材来源**。头发轮廓、肩膀外缘坐标也在这一步量好（戴光环/猫耳/皇冠、画翅膀要用）。

### 2. 定"主导元素家族"（防套路第二道闸）

**已知失败模式（真实案例，必读）**：三张完全不同的女生照片（逆光巷子、黄昏自拍、草原骑马）由三次独立的 skill 调用分别设计，结果全部收敛成同一套——满屏 `flower` + `dotflower` + `sparkle` + `star4`，配一句白色草写 `golden hour`，三张成品几乎无法区分。原因很朴素：面对"女生照片画可爱涂鸦"，最省力的答案永远是撒花撒星加个英文短语，不加约束就会本能滑过去。男生的三张照片因为提示里给了具体方向（手账标注/太空/科技注解），反而各具面貌——**差别不在照片，在有没有硬约束**。

所以动笔前**先从下表选定 1 个主导家族**。选择依据是第 1 步的内容清单：照片里有什么，就选能呼应它的家族。主导家族决定这一版的"讲什么"，外家族元素用来补充和呼吸——**没有数量上限**。

> ⚠️ **这里曾经有过三条数量闸（外家族 ≤4、星 ≤8、文字 ≤4），已被明确移除。** 原因：点名某个具体形状"不许超过 N 个"，本质还是拿具体内容当指令（只是反过来说），而且在花/星真的贴合照片时会误伤。防套路的压力全部由「每个元素必须写出依据」这一条承担——说不出依据的就是通用装饰，写得出的就不是。
>
> **唯一保留的数量硬闸是文字：一版最多一句。** 文字是最响、最能定调的元素，两句起就开始像模板了。而且这一句要求：**英文 + 手写体**（`fname='marker'/'hand'/'note'/'chalk'`）。想说的其他话改成图形表达——手画颜文字（`emo_face`）、拟声线条、爪印轨迹都比第二句文案强。

> ⚠️ **闸是防套路的，不是让你画得少。** 真实反馈：加了这些闸之后有几版只画了 5-6 个元素，用户的评价是"画的内容太少了，我需要更丰富些，更多的元素，更丰富的想象力"。**目标是"丰富且每一笔都有出处"，不是"干净但空"。** 落地要求：
> - **一版画 10-18 个元素组**（留白多的风景/大场景可以到 20+，脸占满画面的特写少些但也不该低于 8）。
> - 从一件实物可以**长出一整组元素**，这才是丰富的来源：一根真晾衣杆 → 挂三件小衣服 + 两个夹子 + 一条延续的线 + 随风的抖动线；一片真叶子 → 叶脉 + 叶柄 + 两片飘落的小叶 + 落地的弹跳弧。**"每个元素都有依据"不等于"每件实物只配一个元素"。**
> - 想象力要往"这件东西接下来会怎样 / 它像什么"发挥：马鬃可以画成被风梳开的线、雨伞可以长出水滴轨迹再溅成小花、影子可以画成小怪物、发梢的光可以拉成流星尾。这些都算有依据，因为起点是实测的真实物体。
> - 元素要有**层次**：大（一两个视觉重心）+ 中（三五个呼应主体的）+ 小（一批点缀：小点、短弧、碎星、气泡）。全是同一尺寸的中等元素最显得空。
> - ⚠️ **别把数量目标堆在同一个锚点上**：为了凑数往一个 0.04 见方的小区域里塞 8 笔，出来是个黄色爪印/爆炸星（真实返工，最后整组删掉）。同类小元素**彼此至少隔 0.05**，凑不够数就少画——`宁缺` 优于挤成一团。密度目标和间距规则要一起读。

### 2.5 笔画粗细必须分层（硬闸，`audit()` 会拦）

真实反馈："画笔要有粗细，不要全是那种细的，现在线条有点太细了，整体加粗一点，然后还需要有更粗的"。**元素数量够了但整幅仍然显得虚，根源几乎总是粗细没拉开**——早期脚本里满屏 `w=2.0~3.2`，那是引擎的旧默认值，不是有意选的。

引擎现在提供**粗细梯子**（按 900px 短边标定，`Doodle` 自动按真实分辨率缩放，所以同一个数字在 900px 自拍和 5712px 单反上视觉一致）：

| 常量 | 值 | 用途 |
|---|---|---|
| `W_HAIR` | 2.5 | 碎发、缝线、腮红、叶侧脉——细节，不承担形体 |
| `W_THIN` | 4.5 | 普通轮廓 |
| `W_MED` | 8.0 | 主要元素的默认粗细（旧脚本的"正常"其实只有这个的 1/3） |
| `W_BOLD` | 16.0 | 视觉重心：大字、主动线、贴纸轮廓 |
| `W_FAT` | 30.0 | 特粗：粗横杠、涂满的形状，一版一两笔 |

比 `W_FAT` 还粗的东西（猫尾巴、绒球、大色块）**直接传数字**，别硬套梯子——毛绒猫尾巴约占图宽 10%，在 900px 短边上就是 `w=70` 左右。

`audit()` 拦两件事：**粗细不足 3 档**、**最粗笔 < W_BOLD**。两条都是"看起来虚"的直接成因。

文字要醒目，**只有一个正确旋钮：`size`**。

⚠️ **别用 `stroke_w` 加粗文字。** 这里的手写体（MarkerFelt 等）本身就是粗体，描边只会把字腔（o/e/a 的孔）填死。实测在干净背景、`size=46` 下：`stroke_w=1.0` 是上限，`2.0` 已经糊住所有孔，`2.5` 把 `posed` 渲成 `poeed`、把 `nobody` 的 o 渲成实心点。**四个独立的代理都为了"让字更清楚"去调它，四个都把文案毁了**（其中一个明确说是照着本文档的例子抄的）——所以引擎现在把 `stroke_w` 硬夹在 1.0 并打印警告。

正确的对应关系记牢：
- 字**不够醒目** → 加大 `size`
- 字在**杂乱背景**上看不清 → 用 `halo=4~5`（在字后垫深色柔光）
- 字在**均匀亮背景**上看不清 → 换位置（`mean` 必须 < 110），或换成饱和色而非白色

写完文字**必须裁出来放大 3 倍 Read 一眼**，逐个字母确认还认得出。这是最容易自欺的一步：缩略图上看着挺好，放大才发现单词已经不成词了。

**但"看不清"和"不够粗"是两个不同的问题，别拿 `stroke_w` 治看不清**：树影、草地这类**斑驳**背景平均亮度看着安全（L=98）但 p90 能到 190，白字会被那些亮斑吃掉。这时候加 `stroke_w` 只是把字母撑成一团（真实返工，`posed` 渲成了 `poeed`——o/s/e 的字腔被描边糊死），正确解法是 `text(..., halo=5)`——在字后垫一层深色柔光把它和背景分开。

**判定白字能不能用，`mean` 和 `p90` 是"与"的关系，两个都要过**：
- `mean` 必须 < 110（绝对亮度够暗）
- **且** `p90 - mean` 要小（没有零散亮斑）

只看其中一个都会翻车，两种翻车我都见过：只看 mean 会被树影里的亮斑打败（要 halo）；**只看离散度会被均匀的亮草地打败**（真实案例：某版量出 p90-mean=16 就判定"安全"，但 mean=109 已经是亮绿草地，白字整片washed out——离散度小只说明"糊得均匀"）。`mean` 过不了就换位置，`mean` 过了但离散度大就上 halo。

⚠️ **`halo` 会吃掉画在它下面的东西**：文字在 `_composite` 里是最后一层，它的深色光晕会盖在已经画好的图形上。所以文字和它的下划线/横杠若有重叠，横杠会被啃出一道暗边。要么让两者不重叠，要么该处别用 halo。

**怎么数"一个元素"**：一个视觉上独立的图形 = 1 个，不论它由几笔或几次 API 调用画成。花冠里的 5 朵花算 5 个；`crown_sketch` 内部十几笔（轮廓+圆圈+缎带）只算 1 个；`frame()` 四条边算 1 个；荧光圈的两次 `arc`（填色+描线）算 1 个；一排 4 个色点作为一组算 1 个；文字连同下划线算 1 个；箭头连同它的标注文字算 1 个。简单说：**观众会当成"一件东西"的就是 1 个**。

**怎么判一个元素属于哪个家族**：**看它依据照片的什么，不看它用哪个图元画的**。在照片里真实的抽绳结上系一个蝴蝶结，即使形状是 bow（表里归在拟物贴纸），它依据的是实测到的那个真结、换张照片就不成立，所以它属于「器物呼应」。反之，一个凭空撒在脸旁的 bow 就是拟物贴纸的通用装饰。判据统一成一句：**这个元素依赖某个实测的照片事实吗？**
**主导家族自己的文案不算外家族**——每个家族都需要配字，标题/短句算在主导家族内，否则两句话就把外家族配额用光了。

**多个家族都合适时怎么选**（下面按顺序判，前一条能决出就不看后面）：

0. **先问：照片里有"正在发生的事"吗？**（起风吹乱头发、她指着什么、他在讲话、马在低头、光正打在脸上）
   - **有** → 优先选能表现那件事的家族（风→天气自然，动作方向→线条动效，握着的东西→器物呼应，说话→涂鸦文字）。**这是最优解**：事件既是事实又自带情绪，内容和气质会同时对上，不用二选一。
   - **没有**（静止站姿、白墙背景、纯摆拍）→ **注意此时"信息量优先"会退化成"给名词贴标签"，做出博物馆展签**。这种照片走装饰性家族（拟物贴纸/星辰光）是**正当的**，别硬凑标注——但每个装饰必须钉在实测位置上（皇冠贴着头发轮廓、星落在真实高光、心画在她真正指的地方），且文案写心里话而不是物件名。
1. **数第 1 步清单里"可指认名词"的个数**：≥4 个 → 选「标注手账」或「器物呼应」，因为只有这两个家族会**消耗名词**，其余家族只会往画面里**添**东西。
   **这两个家族消耗名词的方式不同，别默认选第一个**：「器物呼应」是**延续**实物（把涂鸦接到真东西上——在真晾衣杆上挂几件小衣服、在真抽绳结上系个蝴蝶结、给真叶子补叶脉），「标注手账」是**指着**实物（箭头+标签，站在画面外评论它）。
   - **延续天生更柔和**，因为涂鸦是加入场景；**指向天生更硬**，因为箭头/边框/胶带本身就是硬边工具，装在朦胧柔美的照片上必然读成博物馆展签。
   - 所以：照片朦胧/柔美/梦幻 → 优先「器物呼应」；照片纪实/信息型/硬朗（演讲、登顶、街拍）→「标注手账」。
   - 真实教训：一张暖调逆光柔美人像按"标注手账最百搭"选了它，做出四条引线加裸名词，内容全对但气质全错；换成器物呼应重做后（衣服挂在真杆上 + 领口真结上系蝴蝶结 + 口语文案）气质立刻对了。
   - ⚠️ **「延续」不等于「照着描一遍」**：把实物用同等粗细的平行线再描一次，出来是一条压在实物上的色块（真实返工：一条 W_FAT 黄色板砖横过马颈；改成对称弓形线后又变成 SKILL 自己记录过的叶片形）。**延续 = 把实物的轮廓或运动轨迹往外接出去**（绳子往画外续、鬃毛顺风梳开、车辙延伸、印花从布面飘起来），不是在原地重绘它。同等粗细的重描一律是错的。
2. 照片主体握着/穿着/用着某件有辨识度的东西 → 「器物呼应」。
3. 有明确动作方向 → 「线条动效」；天空占比大 → 「天气自然」。
4. 以上都不成立，才考虑装饰性家族——它们在平手时排最后，**但第 0 条的"静止照片"例外优先于本条**。

> 这条第 0 项来自实测对比：同样在守规则的前提下，一张"起风吹乱头发"的黄昏自拍做出了风线+夕阳+「风好大」（柔美且具体），而一张"只是站着"的逆光人像做成了四条引线标注裸名词（具体但像展签）。**差别不在代理水平，在照片里有没有事件可抓。**

| 主导家族 | 主要图形 | 适用的照片线索 |
|---|---|---|
| 星辰光 | `sparkle` `star4` `star5` `halo` 月牙、星座 `dashed` | **有闸**：夜景、暗背景、舞台光等"光本身就是主角"的照片。**"有逆光光斑"不足以选它**——顺光/柔光人像更不行。选它必须满足：暗背景占比大，且每颗星都精确落在照片里真实存在的光点/光斑上（不是撒在人脸周围） |
| 天气自然 | `sun` `rainbow` `raindrop` 云朵、飞鸟、风线 | 天空占比大的户外、雨天、日落 |
| 标注手账 | `arrow` `text` `frame` `tape` `underline` `highlight` 荧光圈 | 有可指认的实物/事件/场景信息（最百搭，男生照片和纪实场景首选）。**但照片自己已经印满字时要放弃它**——海报标题、幻灯正文、字幕、路牌已经占满了"文字"这个通道，再叠标签是噪声上叠噪声（真实判断：一张发布会演讲照最终改走器物呼应，用麦克风声波+激光笔轨迹+笔电数据流讲同一件事，不加一个标签） |
| 几何图形 | `dot` `arc` `dashed` `poly` 三角/方框/菱形/网格 | 都市、建筑、极简、硬朗、室内 |
| 拟物贴纸 | `crown_sketch` `bow` `heart` `music_note` `paw_print` `cat_ears_sticker` `fur` `emo_face` `pixel_sprite` `blob` | **有闸**：人物有明确可呼应的互动或姿态（指着某处→心画在指尖；歪头卖萌→皇冠戴在头发上；把叶子按脸上→猫须从那片叶子长出去）。**"是个笑着的特写"不足以选它**——那是所有人像的共性，不是这张的特点。**"把人变成小猫"这条线索特别好用**：猫耳锚发际线、猫须锚她手上的东西、尾巴锚身体边缘、爪印锚地面纹理，一整套元素全都有实测依据 |
| 涂鸦文字 | 大字标题、`speech_bubble` 拟声词、手绘表情脸 | 动作夸张、有对话感、可吐槽 |
| 线条动效 | 速度线、拖尾轨迹、爆发光线、`dashed` 路径 | 有动作、有方向感、运动场景 |
| 器物呼应 | 照饰照片里的实物现画（麦克风波纹、镜头光圈、齿轮、火箭、马蹄印） | 照片主体握着/穿着/用着某件有辨识度的东西 |
| 植物花草 | `flower` `dotflower` 叶片、藤蔓、发芽 | **有闸**：仅当人物与植物直接互动时才可作主导——手里拿花、坐在花丛草地上、头挨着花枝。只是背景有绿植**不算**，那种情况请另选家族 |

**硬性禁止**（这些是套路本身，不是"尽量避免"）：

1. 元素与照片零关联——**每个元素都要能说出它依据照片的什么**（画在它旁边、指向它、延续它的形状、表现它正在发生的动作）。说不出的就删，这是唯一真正的防套路闸。
2. 文案使用这些烂熟短语：golden hour、little sunshine、sweet、lovely、my day、good vibes、hello、cutie、dreamy、soft、best day（照片里本身印着这些字则不算）。
3. 文案是抽象形容词或泛泛的情绪词。**文案必须落在照片的具体事实上**：地点、事件、动作、日期、心里话、对话。
   - 好例子：`SUMMIT DAY` / `the way up` / `leaf = whiskers` / `gear: on` / `IoT x GenAI` / `live demo` / `hair went first` / `so windy` / `08.25`
   - 坏例子：`golden hour` / `lovely day` / `so sweet` / `dreamy vibes`
4. **只写物件的名字**——具体但无趣，是另一种失败。`干砌石墙` `一块块石板` 这类读起来像建筑教科书或博物馆展签，不像人写在自己照片上的话。要写就**说出关于它的一件事**：`no cement in this wall` / `leaf = whiskers` / `so windy`。
   对照上面的好例子会发现：`SUMMIT DAY`（事件）、`leaf = whiskers`（她正在做的事）、`the way up`（方向）——几乎没有一个是裸物件名。
5. **一版只许一句文字，英文 + 手写体。** 见上文 2 节的说明。中文能画（引擎自带两款手写中文字体），但英文短句在这种贴纸风里更不出戏；除非用户明确要中文，默认走英文。

**别把反套路做成反美感**：上面这些是防收敛的下限，不是"必须冷冰冰做成说明书"。禁的是**无来由的通用装饰**，不是禁可爱、禁抒情、禁装饰性。

⚠️ **气质匹配和内容匹配同等重要，冲突时不要默认牺牲气质**。真实教训：一张朦胧柔美的暖调逆光人像，被做成了四条引线加裸名词标注的"博物馆展签"——内容呼应满分，气质完全不搭。**硬禁止清单管的是词汇（不许撒花、不许套话），不是情绪**；一张照片完全可以既柔美又具体。要点：

- 文案"落在具体事实上"≠ 只能写物件名。带情绪、俏皮、口语都很好，只要它锚在这张照片的事实上——`回头看了一眼` `第一次骑马，腿软` `这条巷子走了十年` 都合格且有温度；`golden hour` `so sweet` 不合格，因为换张照片照样能用。
- 装饰性元素本身不禁——2 颗星点在真实光斑上、一颗心画在她真正指着的地方、几朵花配在她真的坐着的花丛里，都是好设计。禁的是**撒得满图都是且哪张照片都能撒**。判据仍是那一句：说得出依据吗？
- 若照片本身就柔美、俏皮、少女感，**不必强行改成硬朗手账风**。走"拟物贴纸"或"星辰光"家族完全可以，只要每个元素落点有具体理由（皇冠戴在头发上、心画在指尖旁），而不是在脸周围围一圈。
- 美感优先级高于凑数：与其为满足"≥3 个呼应"硬塞一个丑元素，不如少一个、把已有的做好，然后在自审里写明你的取舍理由。

`references/recipes.md` 里有五个成熟主题的**构图骨架**和踩坑总结（sparkle 参数翻车、underline 斜率匹配等）——**动笔前必读**，但那五个是骨架不是菜单，其中三个都偏少女向且元素池高度重叠，直接照搬正是收敛的来源。

用户要多版时（默认 3-5 版），每版换一个不同的主导家族。

### 3. 写主题脚本
每版一个函数，import 引擎库：

```python
"""涂鸦自审（第 4 步的注释块填在这里，见下文）"""
import sys; sys.path.insert(0, '<skill目录>/scripts')
from doodle_lib import *

d = Doodle('/path/photo.jpg', seed=11)   # 每版换 seed，笔迹抖动不同
# 主导家族=天气自然；每个元素都注明它呼应清单里的哪一项。注意粗细分了 4 档。
d.stroke([(0.72,0.14),(0.79,0.19),(0.76,0.24)], color=WHITE, w=W_BOLD,
         wob=0.6, taper='tip', n=800)     # 主动线（粗）：风线顺她被吹乱的发梢方向
for x0, y0 in ((0.75,0.17), (0.78,0.22)):
    d.stroke([(x0,y0),(x0+0.03,y0+0.01)], color=SKY, w=W_HAIR,
             wob=0.4, n=80, under=False)   # 次级碎线（最细）：疏密层次
d.sun(0.86, 0.52, r=0.028, color=CORAL, face_color=CORAL, under=False)  # 真实夕阳位置
d.text('so windy', 0.16, 0.10, 60, fname='marker', color=WHITE)  # 唯一一句（醒目靠 size，不加 stroke_w）
d.underline(0.13, 0.31, 0.13, color=CORAL, slope=0.004, w=W_FAT)  # 荧光横杠（特粗）
d.audit(family='天气自然', elements=[...], portrait=True)   # 见第 4 步
d.save('out/dusk.jpg')
d.save_video('out/dusk.mp4')              # 默认带二次元手+铅笔叠加层
```

（这段示例本身就符合三条硬闸：零撒花、风线顺真实发丝方向、太阳落在真实夕阳上、只有一句英文手写文案、粗细跨 W_HAIR/W_BOLD/W_FAT 三档以上。**别照抄成模板**——你的照片有别的名词。）

过程视频默认显示一只握素描铅笔的**二次元少女手**（白皙纤细、粉指甲、日系上色）：笔尖锚定当前绘制点，笔画间抬笔飞移（微放大模拟离纸），写字时沿擦入边缘移动，开头从画面右下飞入、结尾飞出。不想要手就 `save_video(path, hand=False)`。

`hand_sprite` 可选：`'anime'`（默认，二次元女生手+黄杆铅笔）、`'pencil'`（欧美卡通手+铅笔）、`'marker'`（粉色马克笔插画手）、`'drawn'`（程序画的卡通拳头，笔杆颜色逐笔跟随涂鸦颜色）、或自备 `(png路径, 笔尖x, 笔尖y)` 元组。`hand_scale` 控制手的大小（默认 0.20 = 手的伸展半径约为短边的 20%，嫌大改小、要醒目改 0.30）。

坐标归一化 (0..1)。**ops 顺序 = 视频绘制顺序**，按"主体装饰 → 环境点缀 → 文字收尾"排。

**引擎 API 速览**（细节读 `scripts/doodle_lib.py` 的 docstring 和方法签名）：
- 基元：`stroke`（brush='marker'/'water'/'crayon'/'glitter'/'highlight'/**'fur'**，color2= 渐变，taper='tip'/'both'）、`arc`、`dashed`、`dot`、`poly`（`wob=` 抖动边缘）、`text`（`halo=` 深色柔光垫底，解决看不清；`stroke_w` 已废弃、夹在 1.0，别用；中文自动切手写体）
- 图案库：`sparkle` `star5` `star4` `heart` `crown` `crown_sketch` `halo` `speech_bubble` `arrow` `rainbow` `sun` `music_note` `paw_print` `cat_ears` `cat_ears_sticker` `butterfly` `tape` `frame` `underline` `raindrop` `flower` `dotflower` `bow` `fur` `blob` `emo_face` `fish` `pixel_sprite`
- **贴纸风的五件新工具**（对标小红书/抖音那种手绘贴纸滤镜）：
  - `fur(pts, w=70, spread=, density=)` 毛绒笔画——猫尾巴、绒球、毛领子。`w` 要给到 40-90（细的毛笔画看不出毛）
  - `blob(x, y, rx, ry, alpha=170)` 软发光团——铺在颜文字/文字底下当白雾，画在最底的 wash 层
  - `emo_face(x, y, eyes='^^', mouth='w', arms='yo')` **手画**颜文字。**不要用字体写颜文字**：系统里能显示 ω ᐢ ˃ ▽ 这些字符的只有印刷黑体（实测只有 Arial Unicode / Hiragino 角ゴ 覆盖），用它会和中文黑体一样出戏；而且颜文字若算文字元素就会占掉那唯一一句的配额。手画的不算文字，还能任意组合
  - `fish(x, y, s=, flip=)` 小鱼；`pixel_sprite(rows, x, y, cell, legend)` 像素贴纸（字符网格，一行里连续同色合成一个矩形，所以视频里是一行行刷出来）
  - `cat_ears_sticker(lx, ly, rx, ry, w_=, h_=)` 实心三角猫耳（`cat_ears` 是描边款）
- 色板：WHITE PINK HOTPINK CORAL PEACH YELLOW LEMON MINT SKY BLUE LAVENDER RED INK；`al(c,alpha)` `dk(c,f)` 调色
- 字体 fname：'marker' 'chalk' 'hand' 'note' 'script' 'savoye' 'comic'；中文手写体 'cjk-kuai'（圆润马克笔感）/ 'cjk-xing'（钢笔行书）/ 'cjk-hei'（印刷黑体，一般别用）

**单位速查**（不看这个必踩坑）：
- 坐标 x/y：归一化 0..1
- `w`（线宽）、`text` 的 `size`：**粗细梯子单位**（按 900px 短边标定，引擎按真实分辨率自动缩放）。用 `W_HAIR`…`W_FAT` 这些常量，别再凭感觉写 `w=2.4`。⚠️ 梯子值会乘 `wscale`：同一个数字在 1920px 图上比 900px 图上粗一倍多——这也是 `stroke_w=2.5` 在大图上格外致命的原因
- `r` / `rx` / `ry` / `dash` / `gap` / `heart` 的 `s`：**占图高的比例**（不是占宽）
- x 方向的偏移量要套 `ax()` 换算，否则圆会被拉扁：`x + d.ax(0.02)`
- `arrow(pts)` 的**箭头画在最后一个点**——决定了你写点的顺序（从标注写向目标）
- 引线末端要**离目标留一点空**（≈0.01），别怼上去；目标位置已有东西时更要提前收笔

**四个必踩的引擎坑**（都来自真实返工）：

1. **长笔画加 `taper` 会串成珠子**：taper 是每个路径点盖一个椭圆，笔锋处半径小于点间距就露出断珠。**解法是加大 `n` 而不是加粗 `w`**（加粗是本能反应，但方向错了，白渲两轮）——长的锥形笔画用 `n≥800`。**长弧线（`r>0.04`）干脆别用 taper**，否则整条弧变成虚线圈（真实案例：一个柔光圈变成了狙击镜十字）。
   **另外：`taper='both'` 在 `W_MED` 以上的宽度会做出"叶片/透镜"形**（两头尖中间鼓），一排速度线全被读成柳叶（真实返工）。**粗笔要收锋就用 `taper='tip'`**（一头粗一头收细，才像马克笔的走势）；`taper='both'` 只适合 `W_THIN` 及更细的短线。
   **光线束不要共起点**：4 条 `taper='tip'` 的射线共用一个原点，出来是一个箭头/爪子，而不是一束光（真实返工：一束黄光读成一个指着她脑袋的大箭头）。把各条的起点错开 0.01-0.02，让它们永不交汇。
   **`taper='tip'` 笔画的实际半高按 `max(0.12, 1-0.85t)` 衰减**，不是名义 `w`：要让一条锥形笔画和别的元素严丝合缝对齐，得按衰减后的宽度算（真实返工两轮：按 W_FAT 名义宽度算，实际那段只有 0.0023-0.0074 高，缝始终合不上）。
   **粗的无 taper 笔画曾经会在边缘出现梳齿状锯齿**（`joint='curve'` 在每个顶点外侧留细缝，宽度一大就看得见）——**引擎已修**（`W_MED` 以上自动给每个内部顶点补圆帽）。留这条记录是因为它容易被误诊成 `n` 或 `wob` 的问题：实测 `n=60` 和 `n=420` 锯齿完全一样，调这两个参数都是白费。
2. **`brush='water'` 大 `w` 走直线出来是硬边胶囊，不是水彩晕**：失败模式是**形状**不是宽度（w=46 出来一根扁平色条，w=26 仍是个鼓出来的色块）。要做柔光晕就用几个同心 `arc` 叠低透明度。
3. **旋转自定义图形时，只用 `ax()` 不够**：要先换算到方形空间旋转、再换算回来，否则圆会变椭圆。
4. **`sun()` 已修但要会用**：以前 7 条光线只覆盖 115°–247°（全指向左侧，明显偏斜）、脸色硬编码成白色（淡色天空上看不见）、深色底影在暖色背景上会发脏。现在 `rays`/`span`/`face_color`/`under` 都是参数——暖调天空建议 `d.sun(x, y, face_color=CORAL, under=False)`，要旧的半边太阳传 `span=(115, 247), rays=7`。
   ⚠️ **`sun()` 的圆盘是描边空心圈，不是实心盘**：背景只要有纹理（头发、草地、砖墙）就会从圈里透出来，读成蜗牛壳（真实返工两轮）。要压在纹理上就用 `poly` 填一个实心半圆盘，`sun()` 只适合纯净留白。这是坑 5「尖角实心形状用 poly 不用 stroke」的同一个道理，适用范围比猫耳更广。
5. **`stroke` 的圆头笔尖会把尖角磨圆**：想画尖角实心形状（猫耳、三角旗、爪子）用 `poly` 填，别用 `stroke` 描折线——描出来是圆拱。真实返工：猫耳用 3 点折线描了两轮，第一版被看成兔耳，加宽后仍是拱形，改 `poly` 才对。
6. **`poly` 填出来的形状要给 `wob`**：不给会得到激光切割般的完美几何形，一眼看出是程序画的。实心贴纸类形状 `wob=1.2~1.5`。
7. **浅色（MINT/PEACH/LEMON）画在亮背景上必须开 `under=True`**：靠深色底影才立得住。`fish` 等图元有 `under=` 参数就是为这个。反过来，**`blob()` 的白光团画在 L>180 的浅色区等于没画**——它只在暗背景上有用。
8. **`brush='highlight'` 的 alpha 只有 95，压在暗区（L<60）会变成一团灰污**：暗背景上的下划线/横杠要用实线马克笔（`stroke(..., w=W_BOLD)`），不要用 highlight。
9. **把光源画成太阳前，先确认它真在画框内**：找到"最暖的一点"就从那里放射光线，会得到一只珊瑚色海星（真实返工）。判据是梯度——若沿地平线的 R-B 是**单调递减**（如 +77→+26），说明峰值在画框外，该画的是**从画外斜切进来的平行光线**而不是一个太阳本体。梯度在画框内出现真正的峰才是点光源。

**画具体物件时，形状可读性是最大的时间坑**（真实案例：一个小衣夹返工三轮——两条腿+横杠被看成十字架，宽的闭合轮廓被看成小口袋，最后窄轮廓+中缝、长宽比 0.42 才读成夹子；一条鱼第一版长宽比 2.3 + 浅尾叉，三条全被读成箭头，改成钝头 + 深尾叉 + 比例 1.6 才成立；猫爪印旧实现是 1 圆掌垫 + 3 颗等距趾垫，画出来是一串气泡，改成宽椭圆掌垫 + 4 颗趾垫、间距 1.5r 才读成爪印）。两条规则：

- **一个物件画成一条闭合轮廓**，不要用几条分开的笔画让观众自己拼——眼睛会拼错。
- **小物件必须给对长宽比**，比例错了会直接读成另一样东西。画完立刻裁切放大 2-3 倍 Read 一眼确认"这看起来像我想画的东西吗"，比渲完整图再看省时间。

**同心弧组（声波、涟漪、光晕、信号）有自己的一套下限**——这类母题是所有"信号/声音/扩散"想法的最终形态，但它极容易塌成别的东西。三次不同的返工都是同一个病：两条半径接近的半圆读成**行李箱提手**，弧线末端两条合并成一个漂浮的**字母 C**，最内圈半径太小直接**糊成一颗豆子**。可用的下限：

- **至少 3 条弧**（2 条永远读成一个物件的轮廓）
- 半径**逐级至少 ×1.4**（挨太近会并成一条粗弧）
- 每条弧的 `r` **≥ 自身线宽的 2.5 倍**（否则弧自己把自己填实）
- 每条弧**至少 80° 张角**（太短的弧是"括号"不是"波"）
- 线宽**由内向外递减**（模拟能量衰减；反过来会读成靶心）

**贴纸尺寸要按"占主体的比例"定，不能凭感觉**：猫耳第一版 `w_=0.050`（耳根宽 0.10）在实测头宽 0.19 的头上像两颗小牙。经验值：猫耳耳根宽 ≈ 头宽的 60-65%、耳高 ≈ 耳根宽的 0.9~1.1。同理**放在窄空带里的元素要按带宽定尺寸**——三条鱼按感觉给 `s=1.5`，结果半长 0.046 超过了那条 0.09 宽空带的余量，右边缘直接切掉半条鱼。

**元素之间也会撞**：两个元素锚在同一个真实物体上时容易叠在一起（真实案例：晾衣线起点正好穿过衣夹，两者各自都对，凑一起就糊了；三滴水对齐到伞骨末端后，又和文字下划线互相压住，三滴挤成一团读成乱涂）。第 5 步的并排比对除了查"元素 vs 照片"，也要查**共用锚点的元素之间**——同类小元素彼此至少隔 0.05。

⚠️ **两个各自都正确的元素会拼成第三个东西**，这比"叠在一起糊掉"更难查，因为分开看两者都合格：一个落点圆点放在一道弹跳弧的正上方，合起来读成一个张开双臂的**火柴人**（真实返工）。所以并排比对时不只要问"它们撞了吗"，还要问**"它们合起来像不像别的东西"**。

**大元素落位前，先实测"人到哪为止"**（这是本 skill 里最贵的一课，一条猫尾巴返工三轮）：

猫尾巴/翅膀/长飘带这类占地大的元素，凭眼看"这块好像是空的"几乎必错——头发、手臂、衣服的边界和你的直觉差很远。三轮返工分别是：起笔压死了她握伞柄的手和两片真叶子 → 移到右侧又压在右臂上（实测 y0.70-0.78 身体一直延伸到 x=0.999）→ 才找到真正的空带。**逐行扫出主体的右缘再落笔**：

```python
# 用"绿"分离人和草地：草地 G-R 为正，皮肤/白衣/黑发都不是
a = np.asarray(Image.open(SRC).convert('RGB'), dtype=float)
idx = np.arange(a.shape[1])
for yn in np.arange(0.50, 1.00, 0.02):
    row = a[int(yn*a.shape[0])]
    body = idx[(row[:,1] - row[:,0] <= 6) & (idx > int(0.55*a.shape[1]))]
    print(f'y={yn:.2f} 主体右缘 x={body.max()/a.shape[1]:.3f}' if len(body) else f'y={yn:.2f} 空')
```

按照片换判据：绿背景用 G-R，天空用 R-B 或亮度，室内白墙用亮度。**衣服是可用画布**（尾巴压在裙摆上完全正常），但脸、手、和照片里正在被使用的物件（她握着的柄、举着的手机）要避开。

⚠️ **单通道阈值会把同亮度的背景物体一起圈进来，这是本 skill 最高频的坑**。真实案例：用 `L<115` 找"她的头发"，结果把她背后同样暗的深绿山丘一起选中了（实测那点 L=88 但 G-B=+17，是山不是头发），三笔风线画到了山坡上；加一条"中性色"判据 `G-B<16` 仍然失败，因为远处山脊也满足。另一例：红笼头污染了"马下颌边缘"的掩膜，咀嚼弧线飘在草地上返工两轮。

**亮度只告诉你「这里暗」，不告诉你「这是什么」——判定一块暗区的身份要用色温，不是亮度。** 真实返工三轮：一条「连续暗带」被当成落在开衫上的发丝，实测 R-B=-10..-24 证明那是 T 恤与开衫的接缝阴影（真发丝是 +5..+11）。

**找一个物体「从哪里开始」，要求连续性，不能只看「阈值命中了」**：某版把布的上缘读成 y=0.110，那其实是屋檐下一小块 15 像素的独立冷色斑；布的连续本体到 y=0.155 才开始（逐行命中数持续 62-75，上方只是零散的 6-37）。判据：**要求连续若干行都有稳定的命中数**，衣夹才不会飘在空档里。

**延续型元素还有一条否决条件：对比度不够就直接放弃。** 同一次返工里那条真发丝 L=185-192、压在 L=219 的奶白开衫上，无论坐标多准都读成一道孤零零的划痕。**|前景−背景| < 30 就早点放弃**，别在坐标上继续调——这是「美感优先级高于凑数」的一个可量化判据。

**所以：任何用来定位元素的掩膜，都必须把掩膜区域裁出来看一眼再用**（`Image.fromarray((mask*255).astype('uint8')).save(...)` 然后 Read 它），或者叠 0.01 网格目视核对。省这一步的代价是两三轮返工。

**引线留空 vs 刻意重叠**：指向型元素（箭头/引线）末端要离目标留 ≈0.01 的空；但**延续型元素必须和真实物体重叠**——挂在真晾衣杆上的线要压住那根杆，留空反而像画错了。指着就留空，接上去就重叠。

⚠️ **但"重叠"不等于"盖住"——盖住实物是净亏损**。真实返工三轮：给麦克风上那圈橙色橡胶握环描一道 `arc`，重叠对了，却把那圈真橙色**闷死了**，还溢到他指节上；改 `W_THIN` 仍把环与麦身之间的缝填成一条实心杠。**照片自己的颜色几乎总比涂鸦好看**，所以延续型元素应该**贴着实物的边缘走、把实物让出来**，而不是压在它上面。

⚠️ **动势线和延续线的规则相反**：延续线要压住实物，但**动势线（风线、拖尾、速度线）必须从实物外面起笔**。真实返工：三条风线的起点落在已画的小衣服下摆内部，读成脱线/破洞而不是风。凡是表现「从某物飘出去」的线，起点要退到该物实测边缘之外。

同一次返工还暴露了另一件事：**小物件要按"逐行量出的形状"选图元，不能只拿 bbox 反推**。那圈橡胶的 bbox 是个方框，我据此推断它是圆环，画了椭圆——逐行测完才发现它是一条**斜带**，任何椭圆都不可能贴合。第 1 步量到 bbox 只是起点，决定用 `arc` 还是 `stroke` 之前要再逐行看一眼真实走向。

**涂鸦能画在人身上吗**：可以，但分区域。脸（五官区）只放极轻的东西（腮红、胡须）或干脆不放；**头发、肩膀、衣服、裙摆都是可用画布**——衣服上画一串 `paw_print`、胸前一颗心、裙摆上压过一条猫尾巴都很正常。别因为怕碰到人就把整个画面中心空着（真实教训：某版因此只在四周留白处画，中间大片浪费）。

**要碰脸就先把五官量出来**，这一步跳不过：

```python
# 眼：脸部区域内左右各自的最暗点；嘴：R-G 最大的那一行；鼻尖：眼与嘴之间的最亮点
for half, (x0, x1) in [('L', (0.50, 0.62)), ('R', (0.62, 0.75))]:
    s = g[int(0.44*H):int(0.50*H), int(x0*W):int(x1*W)]
    i = np.unravel_index(np.argmin(s), s.shape)
    print(f'{half} 眼 x={(x0*W+i[1])/W:.3f} y={(0.44*H+i[0])/H:.3f}')
```

量完把**眼-鼻-嘴连成的三角区标成禁区**，本版任何元素都不进去。真实教训：按"鼻尖在 (0.652,0.515)"贴了个粉色猫鼻，但那是四分之三侧脸，贴纸糊在鼻子上了——**猫鼻这类正脸贴纸在侧脸上不成立，直接删比调位置快**。猫须的须根也要按实测肤色边界放（她的手指在 x0.74-0.78，须根压到 0.744 就骑在指头上了，移到 0.766 才对）。

### 4. 套路自审（防套路第三道闸）

自审是**一次真实的函数调用**，写在 `save()` 之前——不是注释、不是写在回复里：

```python
d.audit(
    family='拟物贴纸',                  # 你选的主导家族
    elements=[                         # 你画的每个元素，逐个列
        # (名称, 是否属于主导家族, 依据照片的什么——必须填，留空会报错)
        ('猫耳×2',        True,  '耳根落在实测发际线上：y=0.33 头发跨 x0.512..0.702'),
        ('毛绒尾巴 w=68', True,  '实测她身体右缘 y0.80 到 x≈0.79，尾巴走右侧那条空带'),
        ('猫须×5',        True,  '从她真实按在脸上的那片绿叶长出（实测绿峰 0.730,0.489）'),
        ('伞骨延续×3',    False, '顺实测伞骨放射中心 hub (0.346,0.320) 续出伞缘'),
        ('「leaf = whiskers」', True, '说的是她正在做的事；落在实测 L=90 的白字安全区'),
    ],
    portrait=True,                     # 人像（影响元素数量下限的提示）
    note='13 组元素、5 档粗细；配色只用冷绿系（实测 G-R=+30~+35）',   # 取舍理由（可选）
)
d.save(...)
```

不通过直接抛异常，只拦三件事：

1. **任何元素的"依据"留空**——留空即等于无来由的通用装饰。这是唯一的内容闸。
2. **文字 > 1 处**——引擎自动实测，谎报无效。
3. **粗细不足 3 档，或最粗笔 < `W_BOLD`**——引擎记录你每一笔的 `w`，自动分档。

数量类的闸（星 ≤N、花 ≤N、外家族 ≤N）**已全部移除**，理由见第 2 节。元素少于 8 组会警告但不拦——特写照片确实该克制，但你得看见它。

### ⚠️ 自审只检查"依据存在"，不检查"依据为真"——这是它最大的漏洞

真实案例（代理自己复查后主动承认的，最有价值的一条反馈）：某版通过了全部三道闸，但其中两个元素的"依据"是**假的**——

- 三条白色风线写着"顺实测发梢斜率"。实测：那条带里发梢像素**每一行都是 0**（纯天空），真发梢在更下面；而且它的斜率 0.43-0.46 与实测发梢轨迹 0.61-2.11 完全不符。
- 一个太阳圆盘写着"圆心贴实测暖峰"。暖峰是真的，但**这张照片里根本没有日轮**（全图最亮 L=247 只有 6 个像素，暖光是从右边框外渗进来的横向暖带）。"有暖光"被悄悄偷换成了"所以有太阳"。

两条"依据"都是**引用了真实数字的通顺句子**，但那些数字并不支持所画的东西。所以：

**凡是声称"顺某个实测方向"或"落在某个实测特征上"的元素，`why` 里必须写进那个数字本身**，写成可当场对照的形式：

```python
('风线×3', True, '斜率 0.88，对照实测发梢轨迹 0.73-0.91；起点 (0.72,0.30) 落在实测暗发像素 L=9-56 上'),
#                  ^^^^ 写成这样，错了会当场看出来
('风线×3', True, '顺实测发梢斜率'),   # ← 这样写等于没写，四个字可以掩盖任何谎
```

自己写完 `why` 再逐条问一遍：**这句话里的数字，我真的量过吗？还是我从别的量测里推断出来的？** 推断出来的就标明是推断。

**另一个相关陷阱：为了让形状可读而反复返工，会让你忘记再问它该不该存在。** 同一个案例里那个太阳返工了四轮（先读成蜗牛、再读成三叉戟），等它终于读得出是太阳时，代理把"可读"当成了"成功"，再没回头问"这张照片里到底有没有太阳"。**任何母题返工 ≥3 轮之后，先重新问一遍它是否有依据，再决定留不留。**

**为什么做成可执行的**：两个代理在冷启动、主动配合的前提下都跳过了这道闸——一个敲下"现在做第 4 步自审："然后直接跑了渲染；另一个自审了 v1 脚本，接着渲了 v2…v5 却**一次都没重审**，它报告的数字描述的是一个已经不存在的脚本（元素构成中途改过）。注释会静默过期，函数调用不会：**改完脚本重跑，自审自动重算**。

自审通过后，还有三个机器测不了的问题要自己回答（写进 `note` 或你的回复）：

- **换照片测试**：蒙掉照片只看涂鸦层，这套元素能贴到任意另一张照片上吗？→ 若"能"，退回第 2 步重做
- **说明书测试**：这版是否为守规则而变得冷冰冰、像展品标签？→ 若"是"，加回温度（口语化文案、贴合动作的小装饰）
- **气质匹配**：照片气质是什么，本版气质是什么，相称吗？→ 柔美梦幻的照片配四条引线标注裸名词，内容再准也是失败的

### 5. 先渲静态图自检，再渲视频

先只跑 `save()`（秒级），用 Read **逐版亲眼检查**：元素有没有挡脸、有没有错位/离人太远、文字下划线平不平行、颜色在该背景上可读吗。改到满意再跑 `save_video()`（约 40-90s/条）。视频抽 2-3 个中间帧确认过程观感（应看到"画到一半"的笔画）。

**凡是声称"指向照片里某个真实东西"的元素，必须并排裁切验证，不许凭眼估坐标。** 细长目标（电线杆、伞骨、缰绳、晾衣绳）眼估误差 0.02 就明显偏了——真实教训：眼估电线杆在 0.636，实测 0.655，差这一点就得返工两轮。

```python
# 定位细长深色目标的真实 x：找最暗的列
import numpy as np; from PIL import Image
g = np.asarray(Image.open(SRC).convert('L'), dtype=float)
band = g[int(0.10*g.shape[0]):int(0.30*g.shape[0]), :]     # 目标所在的纵向区间
col = band.mean(axis=0)
print('最暗列 x =', col.argmin()/g.shape[1])

# ⚠️ 找到后必须再验一步：这条列/行是不是人物自己的边缘？
# 真实翻车：某版在墙上"找到"一条稳定的亮条纹（dev=+85..+139，连续 15 行都在同一列），
# 据此画了两笔——那其实是她自己的袖口/发缘（R-B=+79..+116；真墙只有 +30..+38）。
# 两条判据：
#   (a) 用色温复核身份，不只看亮度偏差（人的皮肤/头发 R-B 远高于石墙）
#   (b) argmax/argmin 每行都落在完全相同的列，是危险信号而不是干净结果——
#       真实的墙面纹理会漂移，纹丝不动的多半是被更强的主体边缘吸住了
row_peaks = [int(band[i].argmax()) for i in range(0, band.shape[0], max(1, band.shape[0]//15))]
print('逐行峰值列（应有漂移）:', row_peaks)

# 验证元素真落在目标上：同区域裁切，成品与原图并排
a = Image.open(OUT).crop(box); b = Image.open(SRC).crop(box)
cv = Image.new('RGB', (a.width*2+8, a.height), 'white')
cv.paste(a, (0,0)); cv.paste(b, (a.width+8, 0)); cv.save('/tmp/cmp.png')   # 然后 Read 它
```

**注意输出目录的沙盒陷阱**：`save()` 打印成功不代表文件真的落盘——沙盒环境下写 `/tmp` 可能被静默丢弃，而你的 `ls` 又看不到它（真实事故：某次连做四轮"渲染+检查"，全程 `save()` 报成功、`ls` 报文件不存在，实际文件写成了但代理看不见，白折腾半小时）。**建议输出到项目目录而非 `/tmp`**；若必须用 `/tmp`，用 `dangerouslyDisableSandbox: true` 跑，并在第一次 `save()` 后立刻 `ls -l` 确认真有文件再继续。

**渲视频务必串行，一次只跑一条**：并发渲染会争抢内存导致 ffmpeg 管道写不完，产出 `moov atom not found` 的截断坏文件（真实事故：6 条并发全废）。渲完立刻校验，坏了就重渲：

```bash
ffprobe -v error -show_entries format=duration -of csv=p=0 out.mp4   # 出时长=好，报 moov atom not found=坏
```

**比并排裁切更强的一道检查：对原图做像素差分**，直接量出"墨水到底落在哪"。用它验证禁区（五官三角、别人的后脑勺、照片自带的文字）真的零墨水，比目视可靠得多：

```python
import numpy as np; from PIL import Image
a = np.asarray(Image.open(SRC).convert('RGB').resize(Image.open(OUT).size), dtype=int)
b = np.asarray(Image.open(OUT).convert('RGB'), dtype=int)
ink = np.abs(a - b).max(axis=2) >= 90      # 阈值必须 ≥90
ys, xs = np.where(ink)
print('ink bbox x %.3f..%.3f y %.3f..%.3f' % (xs.min()/ink.shape[1], xs.max()/ink.shape[1],
                                              ys.min()/ink.shape[0], ys.max()/ink.shape[0]))
# 禁区内墨水数必须是 0
print('五官三角内墨水:', ink[int(0.44*ink.shape[0]):int(0.57*ink.shape[0]),
                            int(0.55*ink.shape[1]):int(0.70*ink.shape[1])].sum())
```

**阈值取 ≥90**：用 34 会把全画面的 JPEG 重压缩噪声当成墨水，得到"处处都是墨"的假结论。

注意渲染中途文件停在几十字节到几 MB 都是正常的（`+faststart` 要等全部帧写完才回写 moov 头），别误判成卡死——看 Python 进程是否还在占 CPU 才准。

### 6. 交付
列出每版文件路径 + 一句话主题说明（点明主导家族和它呼应了照片里的什么）。主动说明可调项：换文字、换色、挪位置、换主导家族、换 seed。

## 依赖与约束

- 需要 PIL(Pillow) + ffmpeg（macOS 系统 python3 + brew ffmpeg 即可），无 GPU/网络/AI 模型依赖
- ⚠️ **量测用 `python3`（Homebrew，带 numpy），渲染两者都行**：系统自带的 `/usr/bin/python3` 有 PIL 但**没有 numpy**，本文档所有实测代码片段在它上面都会 `ModuleNotFoundError`。踩过一次，白排查一轮。
- 视频输出 h264/yuv420p/faststart，奇数尺寸自动裁到偶数
- 大图（>2500px 长边）渲染慢，可 `Doodle(src, max_side=1600)` 降采样
- 引擎是确定性的：同 seed 同输入 → 逐像素相同输出，改脚本重跑安全
